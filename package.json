{
  "name": "@oparadize/root",
  "private": true,
  "scripts": {
    "start": "npx lerna bootstrap && npx lerna run start "
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "dependencies": {
    "@apollo/client": "^3.4.16",
    "cross-env": "^7.0.3",
    "graphql": "^15.7.2",
    "install": "^0.13.0"
  }
}
